
import 'package:flutter/material.dart';

class XTicketsScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Ticktes"),
      ),
    );
  }


}